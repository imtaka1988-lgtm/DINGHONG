# 顶红公众号 / DINGHONG

顶红公众号项目代码仓库。

## 目录

- `app/dinghong-api/`：Spring Boot 后端服务
- `admin/`：当前后台静态页面
- `顶红体育/admin_pages/`：部署包里的后台页面
- `顶红体育/`：部署参考配置与 SQL

## 安全说明

不要提交真实密钥、服务器密码、数据库密码、微信密钥、第三方 API Key 或 GitHub Token。

请优先使用环境变量或服务器本地 `.env` 文件配置敏感信息。参考 `.env.example`。

## 当前导入说明

压缩包内包含 `.git` 历史、部署辅助脚本、构建产物、上传图片和服务器运维脚本。它们不应原样提交到公开仓库。

本分支先补充安全基线文件，并将核心配置改为环境变量方式。后续如要继续清理，请优先处理历史提交中的敏感信息。